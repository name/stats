generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model InputEvent {
  uuid      String   @id
  timestamp DateTime
  eventType String   @map("event_type")
  details   String?

  @@map("input_events")
}
